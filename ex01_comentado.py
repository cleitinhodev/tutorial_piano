# Importa o módulo 'tkinter' e o renomeia como 'tk' para facilitar o uso
import tkinter as tk

# Cria a janela principal da aplicação usando a classe Tk()
janela = tk.Tk()

# Inicia o loop principal da interface gráfica
# Esse loop mantém a janela aberta, aguardando interações do usuário (como cliques)
janela.mainloop()
